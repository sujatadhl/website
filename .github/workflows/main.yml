name: Deploy 

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
         uses: actions/checkout@v2
         with:
          ssh-private-key: ${{ secrets.SSH_Key }}
        -run: 
          https://github.com/sujatadhl/website.git
            
          
                       
